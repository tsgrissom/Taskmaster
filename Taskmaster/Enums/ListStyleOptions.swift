import Foundation

/// Represents the size and style of quick add button the user prefers to appear in the bottom-right of the list UI.
enum QuickAddButtonStyleOption: String, CaseIterable {
    case large, small, material
}
